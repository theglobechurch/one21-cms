<% if m.errors.any? %>
  <div class="message message--error">
    <h2><%= pluralize(m.errors.count, "error") %> prevented this <%= m.class.to_s.downcase %> from being saved</h2>
    <ul>
    <% m.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
